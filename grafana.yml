apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Must match how your Prometheus container is named & exposed internally
    url: http://prometheus:9090
    isDefault: true
